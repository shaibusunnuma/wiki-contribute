// import express from "express";
// import router from './routes/users.js'

// const app = express();

// app.use(express.static('public')) //rendering static pages in folder public

// app.set('view engine', 'ejs'); //set up views engine for html rendering

// app.get('/', (req, res) => {
//     //res.download("server.js"); // download server
//     //res.json({success: true});
//     //res.redirect('/')
//     res.render('index', {title: 'Website'}) //render an html file
// })

// app.use(express.urlencoded({ extended: true})); //allows for accessing post body
// app.use(express.json()) //allows to parse json info from body
// app.use('/users', router);


// app.listen(3000);
console.log('this is working');